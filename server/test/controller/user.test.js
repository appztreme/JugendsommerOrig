'use strict';
const expect = require('expect');
const route = require('./../../routes/userRoute');

describe('User Route', () => {
  it('should get hit', done => {
    expect(1).toEqual(1);
    done();
  });
});
