'use strict';
const expect = require('expect');
const expectExpress = require('expect-express');
expect.extend(expectExpress);
const ctrl = require('./../../controller/registrationCtrl');

describe.only('REGISTRATION ctrl', () => {
    describe('getSelectableEventActivities', () => {

    });
});