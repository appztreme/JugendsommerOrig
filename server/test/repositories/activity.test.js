'use strict';
const expect = require('expect');

const ActivityRepo = require('./../../repositories/activity');

describe('ACTIVITY repo', () => {
    describe('getSelectableEventActivities', () => {
        it('', () => {
            
        });
    });
})